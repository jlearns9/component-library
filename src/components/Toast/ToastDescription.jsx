/* eslint-disable react/prop-types */

// import React from "react";

export default function ToastDescription({ children }) {

    return (
        <div className="toast__description">
            {children}
        </div>
    );
}
